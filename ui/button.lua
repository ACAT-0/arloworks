function UIButtonComponent()
    local group = {

    }

    function group:AppendElement(element)
    end

    function group:Draw()
    end

    function group:GetElement(key)
        return self[key]
    end
end
